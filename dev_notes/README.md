# Developer notes

Internal architecture decisions, audits, release runbooks, and refactor plans.

These files are **not** published to the user-facing documentation site at readthedocs.io. User docs live in `docs/` and are built with MkDocs; configuration is at the repo root in `mkdocs.yml` and `.readthedocs.yaml`.

If a document in this folder becomes relevant to end users, rewrite and move it into `docs/`.
