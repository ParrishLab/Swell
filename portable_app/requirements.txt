# Core dependencies
numpy
opencv-python
tifffile
scipy
scikit-image
pandas
pillow
matplotlib

# Optional but recommended for some TIFFs
imagecodecs

# SAM2 (requires git and internet access)
# If offline, install from a local wheel or cloned repo.
git+https://github.com/facebookresearch/sam2.git

# Torch is platform-specific; install the correct build for your system.
# Example (CPU-only): pip install torch
# See https://pytorch.org/get-started/locally/
